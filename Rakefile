require "rake"
require "dotenv/load"

Rake.add_rakelib(File.expand_path("lib/tasks", __dir__))

# Rakefile for common-backend-ops repository
# all rake tasks are defined in the lib/tasks directory

# To list all available tasks, run:
#   bundle exec rake -T
# To run a specific task, use:
#   bundle exec rake <task_name>
